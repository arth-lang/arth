package conformance;

// @spec: spec.md §10 - Imports (specific and star imports)
// @expect: pass
// @description: Validates import declaration parsing

// Star imports bring all exports into scope
import log.*;

// Specific import brings a single item
// (Using internal types as examples since external may not exist)

module Main {
    public static void main() {
        // Imports are validated at resolution time, not parse time
        // This test validates the parsing is correct
        println("imports: pass");
    }
}
