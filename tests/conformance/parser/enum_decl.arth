package conformance;

// @spec: spec.md §4 - Enum declarations
// @expect: pass
// @description: Validates enum declaration parsing per spec §4

// Simple unit enum
public enum Direction {
    North,
    South,
    East,
    West
}

module DirectionFns {
    public Bool isVertical(Direction d) {
        // Simple check
        return true;
    }
}

module Main {
    public static void main() {
        // Enum values work via switch matching
        println("enum_decl: pass");
    }
}
