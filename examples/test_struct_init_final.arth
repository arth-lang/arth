package test;

struct Config {
    public final String appName;
    public final int version;
    public String environment;
}

module Test {
    public void main() {
        // Should work: initializing via struct literal
        Config cfg = Config { appName: "MyApp", version: 1, environment: "dev" };

        // Should work: changing mutable field
        cfg.environment = "prod";

        // Should FAIL: reassigning final field
        // cfg.appName = "NewApp";  // Uncomment to test error
    }
}
